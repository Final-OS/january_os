# v0.3 分批实施计划

本文用于冻结 `v0.3` 范围，并定义每一批任务的可验收标准。

---

## 目标范围（Scope）

`v0.3` 聚焦于构建完整的用户空间运行环境，使内核能够加载和执行真实用户程序：

1. **VFS 虚拟文件系统** - 统一文件/设备抽象层
2. **块设备层** - virtio-blk 驱动 + 通用块设备抽象
3. **FAT32 文件系统** - 简单可靠的只读/读写文件系统
4. **用户空间基础** - ELF 加载完善 + argv/envp/auxv + 基础 C 库
5. **procfs/sysfs** - 内核状态暴露与调试接口

### v0.3 版本定位

- `v0.3` 的核心任务是打通"磁盘 → 文件系统 → 用户程序"链路
- 使用户能够在 shell 中运行从磁盘加载的真实程序
- 为 v0.4 的网络栈和高级调度提供稳定的用户空间基础

### v0.3 必须完成

- 块设备驱动：virtio-blk 完整读写支持
- 块设备抽象：BlockDevice trait + MBR/GPT 分区解析
- VFS 核心：mount/umount + inode/file + 路径解析
- FAT32 文件系统：只读挂载 + 目录遍历 + 文件读取
- ext4 文件系统：只读挂载 + 文件读取（基础）
- 用户空间完善：argv/envp/auxv 栈帧 + VFS ELF 加载
- procfs：/proc/self + /proc/cpuinfo + /proc/meminfo
- 新增 syscall：lseek/getdents64/dup/dup2/fcntl/chdir/getcwd（80+）

### v0.3 可延期（明确不阻塞 v0.3，但在 v0.4 必须完成）

- 动态链接器（v0.4 必须完成）
- 可写 FAT32/ext4（v0.4 必须完成）
- 完整 POSIX 文件语义（符号链接、硬链接）
- initramfs 支持（v0.4）

---

## 当前状态基线

### 已有能力（v0.2 继承）

| 领域 | 状态 | 关键文件 |
|------|------|----------|
| 静态文件后端 | ✅ 最小可用 | `kernel/src/fs/mod.rs` |
| 文件 syscall | ✅ open/read/close/stat | `kernel/src/syscall/handlers/file.rs` |
| ELF 加载 | ✅ PT_LOAD 映射 + ring3 切换 | `kernel/src/task/exec.rs` |
| 用户地址空间 | ✅ VMA + mm 结构 | `kernel/src/mm/vm/vma.rs` |
| 进程 fd 表 | ✅ per-PID fd 管理 | `kernel/src/fs/mod.rs` |

### 待补能力（v0.3 目标）

| 领域 | 当前状态 | 目标状态 |
|------|----------|----------|
| 块设备 | ❌ 不存在 | virtio-blk 读写 |
| 块设备抽象 | ❌ 不存在 | BlockDevice trait |
| 分区解析 | ❌ 不存在 | MBR/GPT 基础 |
| VFS mount | ❌ 不存在 | mount/umount 基础 |
| 目录操作 | ❌ 不存在 | opendir/readdir |
| FAT32 | ❌ 不存在 | 只读挂载 |
| ELF argv/envp | ⚠️ 未实现 | 完整栈帧 |
| dup/dup2 | ❌ 不存在 | fd 复制 |
| lseek | ❌ 不存在 | 文件偏移 |

---

## 分批计划（Batches）

### Batch 1：块设备抽象与 virtio-blk 驱动

**目标**
- 定义通用 `BlockDevice` trait
- 实现 virtio-blk 驱动（QEMU 兼容）
- 支持基础读写操作

**关键接口**
```rust
pub trait BlockDevice: Send + Sync {
    fn block_size(&self) -> u32;
    fn block_count(&self) -> u64;
    fn read_block(&self, lba: u64, buf: &mut [u8]) -> Result<(), BlockError>;
    fn write_block(&self, lba: u64, buf: &[u8]) -> Result<(), BlockError>;
}
```

**产出文件**
- `kernel/src/drivers/block/mod.rs` - BlockDevice trait
- `kernel/src/drivers/block/virtio_blk.rs` - virtio-blk 实现

**验收条件**
- QEMU 环境下 virtio-blk 设备成功初始化
- 可读写单个扇区并校验数据完整性
- `test block virtio` 回归通过

### Batch 2：分区表解析

**目标**
- 支持 MBR 分区表解析
- 支持 GPT 分区表解析（基础）
- 为每个分区创建 BlockDevice 包装

**关键结构**
```rust
pub struct Partition {
    pub index: u32,
    pub start_lba: u64,
    pub block_count: u64,
    pub part_type: PartitionType,
}

pub struct PartitionedDevice {
    device: Arc<dyn BlockDevice>,
    partitions: Vec<Partition>,
}
```

**产出文件**
- `kernel/src/drivers/block/partition.rs`
- `kernel/src/drivers/block/mbr.rs`
- `kernel/src/drivers/block/gpt.rs`

**验收条件**
- 正确解析 QEMU virtio-blk 的 MBR/GPT 分区
- `test block partition` 回归通过

### Batch 3：VFS 核心抽象

**目标**
- 定义 VFS 核心接口（SuperBlock, Inode, File, Dentry）
- 实现 mount/umount 管理
- 路径解析与命名空间

**关键接口**
```rust
pub trait FileSystem: Send + Sync {
    fn name(&self) -> &str;
    fn root(&self) -> Arc<dyn Inode>;
    fn sync(&self) -> Result<(), i32>;
}

pub trait Inode: Send + Sync {
    fn ino(&self) -> u64;
    fn mode(&self) -> InodeMode;
    fn size(&self) -> u64;
    fn read_at(&self, offset: u64, buf: &mut [u8]) -> Result<usize, i32>;
    fn lookup(&self, name: &str) -> Result<Arc<dyn Inode>, i32>;
    fn readdir(&self, cb: &mut dyn FnMut(&DirEntry) -> bool);
}

pub trait File: Send + Sync {
    fn inode(&self) -> Arc<dyn Inode>;
    fn pos(&self) -> u64;
    fn seek(&mut self, whence: SeekWhence, offset: i64) -> Result<u64, i32>;
}
```

**产出文件**
- `kernel/src/fs/vfs/mod.rs` - VFS 核心
- `kernel/src/fs/vfs/inode.rs` - Inode trait
- `kernel/src/fs/vfs/file.rs` - File trait
- `kernel/src/fs/vfs/mount.rs` - mount 管理
- `kernel/src/fs/vfs/path.rs` - 路径解析

**验收条件**
- 可挂载/卸载文件系统
- 路径解析正确处理 `.` `..` 和绝对/相对路径
- `test vfs` 回归通过

### Batch 4：FAT32 文件系统（只读）

**目标**
- 解析 FAT32 BPB 和 FSInfo
- 实现目录遍历和文件读取
- 接入 VFS 接口

**关键结构**
```rust
pub struct Fat32Super {
    device: Arc<dyn BlockDevice>,
    bpb: BiosParameterBlock,
    fat: Vec<u32>,
    root_cluster: u32,
}

pub struct Fat32Inode {
    super: Arc<Fat32Super>,
    entry: FatDirEntry,
    first_cluster: u32,
}
```

**产出文件**
- `kernel/src/fs/fat/mod.rs`
- `kernel/src/fs/fat/bpb.rs`
- `kernel/src/fs/fat/dir.rs`
- `kernel/src/fs/fat/file.rs`

**验收条件**
- 成功挂载 FAT32 格式的磁盘镜像
- 可读取根目录和子目录内容
- 可读取文件内容并校验
- `test fat32` 回归通过

### Batch 5：VFS syscall 接入

**目标**
- 将现有 `fs::open/read/close` 替换为 VFS 后端
- 实现 `lseek/getdents/statfs` 等 syscall
- 更新 per-process fd 表以支持 File trait 对象

**关键改动**
- `OpenFile` 从 enum 改为 `Arc<dyn File>` + 元数据
- `sys_open` 调用 `vfs::open(path, flags)`
- `sys_read` 调用 `file.read_at(pos, buf)`
- `sys_lseek` 新增实现

**验收条件**
- 现有 `open/read/close` 测试继续通过
- `lseek` 功能正确
- 管道和静态文件后端继续工作

### Batch 6：ELF 加载完善

**目标**
- 从 VFS 加载 ELF 文件（替代静态文件后端）
- 实现 argv/envp/auxv 栈帧构建
- 支持执行磁盘上的用户程序

**关键改动**
- `execve` 从 `fs::read_static_file` 改为 `vfs::open + read`
- 栈布局：
  ```
  [padding for 16-byte alignment]
  [auxv entries]
  [envp pointers]
  [argv pointers]
  [argc]
  ```

**产出文件**
- `kernel/src/task/exec.rs` - 更新加载逻辑
- `kernel/src/task/stack.rs` - 新增栈帧构建

**验收条件**
- 可执行 FAT32 分区上的 ELF 程序
- 程序正确接收 argc/argv/envp
- `test task usermode` 更新并通过

### Batch 7：procfs 基础实现

**目标**
- 挂载 `/proc` 文件系统
- 暴露 `/proc/self`, `/proc/[pid]/`, `/proc/cpuinfo`, `/proc/meminfo`

**关键结构**
```rust
pub struct Procfs {
    pid: Option<Pid>,
}

pub struct ProcInode {
    fs: Arc<Procfs>,
    entry: ProcEntry,
}

enum ProcEntry {
    SelfLink,
    PidDir(Pid),
    Cmdline(Pid),
    Status(Pid),
    CpuInfo,
    MemInfo,
}
```

**产出文件**
- `kernel/src/fs/proc/mod.rs`
- `kernel/src/fs/proc/inode.rs`

**验收条件**
- `/proc/self` 正确指向当前进程
- `/proc/cpuinfo` 显示 CPU 信息
- `/proc/meminfo` 显示内存信息
- `cat /proc/cpuinfo` 在 shell 中可用

### Batch 8：用户空间集成测试

**目标**
- 创建可启动的磁盘镜像（含 FAT32 + 用户程序）
- 端到端测试：启动 → 挂载 → 执行程序
- 编写简单的 hello world 用户程序

**产出**
- `target/user_disk.img` - 可启动磁盘镜像
- `userland/hello/` - 简单用户程序
- 集成测试脚本

**验收条件**
- 从磁盘启动并挂载 FAT32 分区
- 执行 hello world 程序输出正确
- `make test-integration` 通过

### Batch 9：文档与回归收口

**目标**
- 更新 API 文档
- 补充 VFS/FAT32 实现文档
- 回归测试覆盖所有新增功能

**验收条件**
- 文档与实现一致
- 所有 `test block/vfs/fat` 回归通过
- QEMU + VMware 启动验证

---

## 系统调用扩展计划

### v0.3 新增 syscall

| syscall | nr | Batch | 说明 |
|---------|-----|-------|------|
| `lseek` | 8 | 5 | 文件偏移设置 |
| `getdents64` | 217 | 5 | 目录遍历 |
| `mkdir` | 83 | 延期 | 目录创建 |
| `rmdir` | 84 | 延期 | 目录删除 |
| `unlink` | 87 | 延期 | 文件删除 |
| `statfs` | 138 | 5 | 文件系统信息 |
| `fstatfs` | 139 | 5 | fd 文件系统信息 |
| `dup` | 32 | 5 | fd 复制 |
| `dup2` | 33 | 5 | fd 复制到指定 |
| `dup3` | 292 | 5 | dup2 + flags |
| `fcntl` | 72 | 5 | fd 控制 |

### v0.3 完善 syscall

| syscall | 当前状态 | 目标 |
|---------|----------|------|
| `open` | O_RDONLY only | O_RDWR, O_CREAT, O_DIRECTORY |
| `execve` | 静态文件后端 | VFS 文件路径 |

---

## 依赖关系图

```
Batch 1 (BlockDevice + virtio-blk)
    ↓
Batch 2 (Partition)
    ↓
Batch 3 (VFS Core) ←─────────────┐
    ↓                            │
Batch 4 (FAT32)                  │
    ↓                            │
Batch 5 (VFS syscall) ───────────┘
    ↓
Batch 6 (ELF 完善)
    ↓
Batch 7 (procfs) ←── 可并行
    ↓
Batch 8 (集成测试)
    ↓
Batch 9 (文档收口)
```

---

## 统一验收命令基线

- `make build` - 构建通过
- `make run` - QEMU 启动正常
- `test block` - 块设备测试通过
- `test vfs` - VFS 测试通过
- `test fat` - FAT32 测试通过
- `test all` - 全量回归通过

---

## 执行状态

- [ ] Batch 1：块设备抽象与 virtio-blk 驱动
- [ ] Batch 2：分区表解析
- [ ] Batch 3：VFS 核心抽象
- [ ] Batch 4：FAT32 文件系统（只读）
- [ ] Batch 5：VFS syscall 接入
- [ ] Batch 6：ELF 加载完善
- [ ] Batch 7：procfs 基础实现
- [ ] Batch 8：用户空间集成测试
- [ ] Batch 9：文档与回归收口

---

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| virtio-blk 复杂度超预期 | 阻塞后续批次 | 先实现最简 PIO 模式，MSI-X 后续优化 |
| FAT32 规范复杂 | 实现周期长 | 先只读，限制支持的 FAT 类型（仅 FAT32） |
| VFS 设计过度 | 实现复杂 | 参考 Linux minix fs 简化，不追求完整 POSIX |
| ELF 栈帧兼容性 | 用户程序运行失败 | 参考 Linux x86_64 ABI 规范，添加测试用例 |

---

## v0.3 后续优化积压（非阻断）

### O1：ext4 文件系统

- 范围：ext4 只读挂载
- 版本归属：`v0.3.1`

### O2：动态链接器

- 范围：.so 加载 + PLT/GOT + 符号解析
- 版本归属：`v0.3.2` 或 `v0.4.0`

### O3：可写 FAT32

- 范围：文件/目录创建、删除、修改
- 版本归属：`v0.3.1`

### O4：sysfs 实现

- 范围：设备模型 + 属性文件
- 版本归属：`v0.3.2`
