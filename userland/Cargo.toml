[workspace]
resolver = "2"
members = [
    "runtime",
    "coreutils",
    "sh",
    "ls",
    "cat",
    "pwd",
    "echo",
    "forktest",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"
